extends layout

block content
  h1= title
  
  div(class="container-fluid")
        div(class="row")
            -
                let parking=results 
            each p,ink in parking 
                div(class="col-sm-2" onclick=`buy(${ink})` ) #{p.type}
                div(class="col-sm-2" ) #{p.ticket}
                div(class="col-sm-2" ) #{p.capacity}


  script.
     
       function buy(a)
       {
         alert(`You purchased Item ${a+1}`)
       }